# -p, --cms-path <cmsPath>', 'CMS server absolute path
cmsPath = "/admin"

# '-l, --repo-location <repoLocation>', 'repo root location'
repoLocation = "examples/site"

# '-c, --cms-location <cmsLocation>', 'CMS app location'
cmsLocation = "examples/site/admin"

# '-d, --dist-location <distLocation>', 'CMS build location'
distLocation = "examples/site/admin"

# port the server will run on localhost
port = 8090
